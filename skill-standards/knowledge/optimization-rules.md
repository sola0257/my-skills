# 📚 Skill 优化规则知识库

> 本文档详细说明 skill-optimizer 使用的 11 条优化规则（10条内置 + 1条学习提取），包括规则来源、检测逻辑和修复方案。

---

## 规则来源

这些规则从以下 Skills 的迭代优化过程中提炼：

| 来源 Skill | 版本 | 提炼的规则 |
|------------|------|------------|
| xiaohongshu-content-generator | v5.1 | R01, R02, R03, R04, R06 |
| video-script-generator | v2.1 | R02, R05, R08 |
| content-production-pipeline | v1.2 | R01, R05, R10, **R11** |
| knowledge-extractor | v3.0 | R06 (Token成本优化) |
| wechat-content-generator | v3.1 | R02, R07 |
| topic-discovery | v3.0 | R04, R09 |

---

## R01: 静默执行协议 🔴

### 规则说明
所有 Skill 必须包含静默执行协议，禁止在执行过程中询问确认或等待用户输入。

### 问题场景
```
❌ Skill 执行到一半停下来问"是否继续？"
❌ Skill 询问"请问您想要多少个选题？"
❌ Skill 等待用户确认后才进行下一步
```

### 必需内容
SKILL.md 必须包含以下区块（位于 frontmatter 之后的首个主要章节）：

```markdown
## 🚨 静默执行协议（必须遵守）

**执行规则：**
- ❌ **禁止** 请求确认以继续
- ❌ **禁止** 询问"是否需要更多信息"
- ❌ **禁止** 中途停顿等待用户反馈
- ✅ **必须** 一次性生成完整输出
- ✅ **必须** 数据缺失时使用默认值
- ✅ **必须** 遇到错误时静默处理并继续
```

### 检测正则
```python
SILENT_PROTOCOL_PATTERNS = [
    r"禁止.*请求确认",
    r"禁止.*询问",
    r"禁止.*中途停顿",
    r"必须.*一次性",
    r"必须.*默认值",
    r"必须.*静默处理"
]
```

### 自动修复
在 frontmatter 后插入标准协议区块。

---

## R02: 输出模板完整性 🟡

### 规则说明
每个 Skill 应定义清晰的输出模板，使用 Markdown 代码块包裹，包含所有必需字段。

### 问题场景
```
❌ 输出格式每次都不一样
❌ 缺少关键字段（如配图信息表）
❌ 没有明确的输出结构定义
```

### 必需内容
- 章节标题包含"输出模板"或"Output Template"
- 使用 ```markdown ``` 代码块包裹示例
- 包含占位符说明

### 检测逻辑
```python
def check_output_template(content):
    has_section = re.search(r'##.*输出模板|Output Template', content)
    has_codeblock = re.search(r'```markdown[\s\S]*?```', content)
    return has_section and has_codeblock
```

---

## R03: Anti-Patterns 定义 🟡

### 规则说明
每个 Skill 应明确定义禁止行为，帮助 LLM 避免常见错误。

### 问题场景
```
❌ LLM 不知道什么是错误做法
❌ 相同错误反复出现
❌ 缺乏具体的错误示例
```

### 必需内容
```markdown
## ❌ Anti-Patterns（禁止行为）

**禁止示例1：[问题名称]**
```
❌ [错误代码或行为示例]
```
→ 应[正确做法]

**禁止示例2：...**
```

### 检测逻辑
```python
def check_anti_patterns(content):
    has_section = re.search(r'##.*Anti-Pattern|禁止行为', content)
    has_examples = content.count('❌') >= 3
    return has_section and has_examples
```

---

## R04: 临时文件规范 🟢

### 规则说明
所有临时文件必须保存到 `.tmp/` 目录，任务完成后自动清理。

### 问题场景
```
❌ 临时文件散落在工作目录中
❌ 测试文件没有被清理
❌ 临时文件与正式文件混在一起
```

### 必需内容
```markdown
## 📁 临时文件管理

**规则：**
- ✅ 所有测试/临时文件必须保存到 `.tmp/` 文件夹
- ✅ 任务完成后自动清理
- ❌ 禁止临时文件散落在工作目录中

**目录结构：**
```
[输出目录]/
├── [正式文件]
└── .tmp/
    ├── test_*.png
    └── draft_*.md
```
```

### 检测逻辑
```python
def check_temp_file_rules(content):
    return '.tmp/' in content or '临时文件' in content
```

### 自动修复
在文件末尾添加标准临时文件管理章节。

---

## R05: 错误处理表 🟡

### 规则说明
每个 Skill 应使用表格形式定义常见错误场景的处理策略。

### 问题场景
```
❌ 遇到错误不知道如何处理
❌ 错误处理策略不一致
❌ 缺少错误恢复机制
```

### 必需内容
```markdown
## 🛠️ 错误处理

| 场景 | 处理方式 |
|------|----------|
| 输入模糊 | 自动细化或使用默认值 |
| API 超时 | 重试1次后跳过 |
| 文件不存在 | 自动创建目录 |
| 生成失败 | 跳过并继续下一个 |
```

### 检测逻辑
```python
def check_error_handling(content):
    has_section = re.search(r'##.*错误处理', content)
    has_table = re.search(r'\|.*场景.*\|.*处理.*\|', content)
    return has_section and has_table
```

---

## R06: Token 成本意识 🟡

### 规则说明
涉及大量文本处理的 Skill 应包含成本优化策略。

### 问题场景
```
❌ 每次都全量读取大文件
❌ 重复处理已分析的内容
❌ 不必要地消耗大量 Token
```

### 最佳实践
```markdown
**成本优化策略：**
1. **本地预处理**：Python 脚本提取文本，降低 80-90% 成本
2. **增量处理**：已处理文件不重复分析
3. **索引缓存**：建立索引快速定位，避免全量扫描
```

### 检测逻辑
```python
def check_token_optimization(content):
    keywords = ['成本', 'Token', '预处理', '增量', '缓存']
    # 仅对大文本处理类 Skill 强制要求
    if is_text_heavy_skill(content):
        return any(kw in content for kw in keywords)
    return True  # 非文本密集型 Skill 跳过
```

---

## R07: 知识库引用一致性 🔴

### 规则说明
所有知识库引用路径必须有效，跨 Skill 引用必须使用正确格式。

### 问题场景
```
❌ 引用的 knowledge/*.md 文件不存在
❌ 文件路径拼写错误
❌ 相对路径和绝对路径混用
```

### 引用格式标准
```markdown
# 同目录引用（(推荐)
[文件名](knowledge/filename.md)

# 跨 Skill 引用（必须使用绝对路径）
[文件名](file:///Users/dj/Desktop/小静的skills/other-skill/knowledge/file.md)
```

### 检测逻辑
```python
def check_knowledge_refs(skill_path, content):
    refs = re.findall(r'\[.*?\]\(((?:file://)?[^)]+\.md)\)', content)
    invalid = []
    for ref in refs:
        full_path = resolve_path(skill_path, ref)
        if not os.path.exists(full_path):
            invalid.append(ref)
    return len(invalid) == 0, invalid
```

---

## R08: 版本历史记录 🟢

### 规则说明
每个 Skill 应记录版本更新历史，便于追溯变更。

### 问题场景
```
❌ 不知道 Skill 更新了什么
❌ frontmatter 版本与正文不一致
❌ 缺少变更日志
```

### 必需内容
```markdown
## 📝 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v2.0 | 2026-01-15 | 新增XX功能，优化YY逻辑 |
| v1.0 | 2026-01-10 | 初始版本 |
```

### 检测逻辑
```python
def check_version_history(content):
    # 检查 frontmatter 版本
    fm_version = re.search(r'version:\s*["\']?(\d+\.\d+)', content)
    # 检查版本历史表
    has_history = re.search(r'##.*版本.*历史', content)
    return fm_version and has_history
```

### 自动修复
在文件末尾添加版本历史章节，使用 frontmatter 中的版本号。

---

## R09: 默认值定义 🟡

### 规则说明
所有用户输入都应定义缺失时的默认值，确保 Skill 不因数据缺失而中断。

### 问题场景
```
❌ 用户未指定数量时不知道生成多少
❌ 用户未指定平台时询问确认
❌ 缺少输入后无法继续执行
```

### 必需内容
```markdown
**默认值规则：**
- 未指定时间 → 默认近7天
- 未指定平台 → 默认小红书
- 未指定数量 → 默认10个
- 粉丝数未知 → 默认起号阶段
```

### 检测逻辑
```python
def check_default_values(content):
    keywords = ['默认', '未指定', '如未提供', 'default']
    return any(kw in content for kw in keywords)
```

---

## R10: Skill 协作声明 🟡

### 规则说明
声明与其他 Skills 的依赖关系，方便维护和问题排查。

### 问题场景
```
❌ 不知道 Skill 依赖了哪些其他 Skills
❌ 依赖的 Skill 被删除后报错
❌ 版本不兼容导致问题
```

### 必需内容
```markdown
## 🔗 依赖 Skill

| 技能 | 版本要求 | 用途 | 状态 |
|------|----------|------|------|
| product-catalog | - | 商品库查询 | ✅ 已部署 |
| topic-discovery | v2.0+ | 选题发现 | ✅ 已部署 |
```

### 检测逻辑
```python
def check_skill_dependencies(content, skills_dir):
    # 提取依赖列表
    deps = re.findall(r'\|\s*([a-z\-]+)\s*\|', content)
    # 检查每个依赖是否存在
    for dep in deps:
        dep_path = os.path.join(skills_dir, dep)
        if not os.path.exists(dep_path):
            return False, f"依赖的 Skill '{dep}' 不存在"
    return True, None
```

---

## R11: 组合Skill同步 🔴（学习提取）

> **来源**：从 `content-production-pipeline` 及其分支 Skills 协作优化中提取
> **提取日期**：2026-01-15

### 规则说明
修改组合类 Skill 时，必须确保规则变更同步到所有分支 Skills，且分支 Skill 能独立运行。

### 问题场景
```
❌ 在组合 Skill 中添加 API 禁用规则，但分支 Skill 单独调用时没有该限制
❌ 组合 Skill 添加了错误重试逻辑，分支 Skill 没有同步
❌ 分支 Skill 依赖组合 Skill 的上下文才能正常工作
```

### 适用场景
```
组合类 Skill 示例：
- content-production-pipeline（组合）
  ├── topic-discovery（分支）
  ├── xiaohongshu-content-generator（分支）
  ├── wechat-content-generator（分支）
  └── video-script-generator（分支）

- product-pipeline（组合）
  ├── product-selector（分支）
  └── product-optimizer（分支）
```

### 核心原则

1. **修复/优化必须同步到分支 Skills**
   - 组合 Skill 中的规则变更 → 必须同步到各分支 Skill
   - 分支 Skill 单独调用时也能按优化后的规则执行

2. **分支 Skill 要能独立运行**
   - 分支 Skill 不依赖组合 Skill 才能正常工作
   - 质量验证、错误处理逻辑必须写在分支 Skill 中

### 同步检查清单

| 修改类型 | 必须同步到 |
|----------|-----------|
| API 禁用规则（如禁止 DALL-E） | 所有使用该 API 的分支 Skills |
| 错误处理/重试逻辑 | 所有分支 Skills |
| 输出格式/清洗规则 | 所有生成内容的分支 Skills |
| 质量验证步骤 | 所有分支 Skills |

### 检测逻辑
```python
def check_composite_sync(skill_name, skill_content):
    """
    检查组合 Skill 是否满足同步规则
    """
    # 识别是否为组合 Skill
    composite_indicators = ['Pipeline', '流水线', '组合', '依赖 Skill']
    is_composite = any(ind in skill_content for ind in composite_indicators)
    
    if not is_composite:
        return True, "非组合 Skill，跳过检查"
    
    # 获取分支 Skills 列表
    branch_pattern = r'├──\s*([a-z\-]+)|\|\s*([a-z\-]+)\s*\|.*依赖'
    branches = re.findall(branch_pattern, skill_content)
    
    issues = []
    for branch in branches:
        branch_name = branch[0] or branch[1]
        # 检查分支是否存在
        branch_path = f"/Users/dj/Desktop/小静的skills/{branch_name}/SKILL.md"
        if not os.path.exists(branch_path):
            issues.append(f"分支 {branch_name} 不存在")
            continue
        
        # 检查分支是否有独立执行能力
        branch_content = open(branch_path).read()
        if '静默执行协议' not in branch_content:
            issues.append(f"分支 {branch_name} 缺少静默执行协议")
    
    return len(issues) == 0, issues
```

---

## 规则优先级

当多个规则冲突时，按以下优先级处理：

1. **R01 静默执行协议** - 最高优先级，不可妥协
2. **R11 组合Skill同步** - 影响多 Skill 协作正确性
3. **R07 知识库引用** - 影响功能正常运行
4. **R05 错误处理** - 影响稳定性
5. **R06 Token 成本** - 影响效率和成本
6. **其他规则** - 按检测顺序处理

---

## 规则更新记录

| 日期 | 更新内容 |
|------|----------|
| 2026-01-15 | 新增 R11 组合Skill同步规则（学习提取） |
| 2026-01-15 | 初始版本，10 条内置规则 |

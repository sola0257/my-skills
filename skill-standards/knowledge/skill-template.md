# 📐 Skill 标准模板

> 新建任何 Skill 时，必须按照此模板结构生成 SKILL.md

---

## Frontmatter（必须）

```yaml
---
name: [skill-name]           # kebab-case 格式
description: |
  [Skill 的一句话描述]
  
  触发场景：
  - "[触发词示例1]"
  - "[触发词示例2]"
license: MIT
version: "1.0"
---
```

---

## 完整模板

```markdown
---
name: [skill-name]
description: |
  [Skill 描述]
  
  触发场景：
  - "[触发词1]"
  - "[触发词2]"
license: MIT
version: "1.0"
---

# [Skill 名称] v1.0

## 🚨 静默执行协议（必须遵守）

**执行规则：**
- ❌ **禁止** 请求确认以继续
- ❌ **禁止** 询问"是否需要更多信息"
- ❌ **禁止** 中途停顿等待用户反馈
- ✅ **必须** 一次性生成完整输出
- ✅ **必须** 数据缺失时使用默认值
- ✅ **必须** 遇到错误时静默处理并继续

---

## 📋 概述

用户提供 [输入] 后，自动完成以下任务：
1. [任务1]
2. [任务2]
3. [任务3]

---

## ⚙️ 配置信息

### 路径配置
```
[相关路径配置]
```

### API 配置（如需要）
```
[API 端点、密钥等]
```

### 默认值规则
- 未指定 [参数A] → 默认 [值A]
- 未指定 [参数B] → 默认 [值B]

---

## 🔄 执行流程

### Step 1: [步骤名称]
[步骤详细说明]

### Step 2: [步骤名称]
[步骤详细说明]

### Step 3: [步骤名称]
[步骤详细说明]

---

## 📄 输出模板

```markdown
# [输出标题]

> [摘要/描述]

---

## [内容区块]

[输出内容结构示例]

---

## [附加信息]

| 字段 | 值 |
|------|-----|
| [字段1] | [值1] |
```

---

## ✅ Good Case（正确示例）

**用户输入：**
```
[示例输入]
```

**正确执行：**
1. ✅ [正确行为1]
2. ✅ [正确行为2]
3. ✅ [正确行为3]

---

## ❌ Anti-Patterns（禁止行为）

**禁止示例1：[问题名称]**
```
❌ [错误行为示例]
```
→ 应[正确做法]

**禁止示例2：[问题名称]**
```
❌ [错误行为示例]
```
→ 应[正确做法]

---

## 🛠️ 错误处理

| 场景 | 处理方式 |
|------|----------|
| 输入模糊 | 自动细化或使用默认值 |
| API 超时 | 重试1次后跳过 |
| 文件不存在 | 自动创建目录 |
| 生成失败 | 跳过并继续下一个 |

---

## 📁 临时文件管理

**规则：**
- ✅ 所有测试/临时文件必须保存到 `.tmp/` 文件夹
- ✅ 任务完成后自动清理
- ❌ 禁止临时文件散落在工作目录中

**目录结构：**
```
[输出目录]/
├── [正式文件]
└── .tmp/
    ├── test_*.png
    └── draft_*.md
```

---

## 🔗 依赖 Skill

| 技能 | 版本要求 | 用途 | 状态 |
|------|----------|------|------|
| [技能名] | [版本] | [用途] | ✅ 已部署 |

> 如无依赖，可写：无依赖，独立运行

---

## 📝 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | [YYYY-MM-DD] | 初始版本 |
```

---

## 必需章节检查清单

新建 Skill 时，确保包含以下章节：

| 章节 | 必须 | 说明 |
|------|------|------|
| Frontmatter | ✅ | name, description, version |
| 静默执行协议 | ✅ | 禁止中断规则 |
| 概述 | ✅ | 功能说明 |
| 配置信息 | ✅ | 路径、API、默认值 |
| 执行流程 | ✅ | Step by step |
| 输出模板 | ✅ | Markdown 代码块包裹 |
| Good Case | ✅ | 正确示例 |
| Anti-Patterns | ✅ | 禁止行为 |
| 错误处理 | ✅ | 错误处理表 |
| 临时文件管理 | ⚠️ | 推荐 |
| 依赖 Skill | ⚠️ | 如有依赖必须声明 |
| 版本历史 | ✅ | 版本更新记录 |

---

## 快速生成命令

```bash
# 创建 Skill 目录
mkdir -p /Users/dj/Desktop/小静的skills/[skill-name]/knowledge

# 创建符号链接
ln -s /Users/dj/Desktop/小静的skills/[skill-name] /Users/dj/.claude/skills/[skill-name]
```

---
name: content-production-pipeline
description: "智能内容生产流水线。自动完成：选题发现 → 平台选题分配 → 小红书内容生成 → 公众号内容生成 → 视频脚本生成。支持批量处理、进度追踪、错误恢复。同一天不同平台使用不同选题。"
license: MIT
version: "1.4"
---

# 🏭 智能内容生产流水线 v1.4

## 🚨 静默执行协议（必须遵守）

**执行规则：**
- ❌ **禁止** 请求确认以继续
- ❌ **禁止** 询问"是否需要更多信息"
- ❌ **禁止** 中途停顿等待用户反馈
- ✅ **必须** 一次性完成整个流水线
- ✅ **必须** 使用 TodoWrite 追踪进度
- ✅ **必须** 遇到错误时记录并继续处理下一个

---

## 🧠 LLM 执行策略（必读）

> **⚠️ 重要：Skills 是指令文档，不是异步任务系统！**
>
> - ❌ 不能"调用 Skill 并等待完成"
> - ❌ 不能用 `Task Output skill-name` 获取结果
> - ✅ 必须**直接按照各 Skill 的指令执行**所有步骤

**正确执行方式：**

| 阶段 | 执行方式 |
|------|----------|
| 选题发现 | 直接用 `search_web` 搜索热点，分析趋势，匹配商品，生成报告 |
| 小红书生成 | 直接生成文案，调用云雾 API 生成配图，保存文件 |
| 公众号生成 | 直接生成文案、配图，推送草稿箱 |
| 视频脚本 | 直接生成分镜脚本和逐字稿 |

---

## 📋 概述

**单一 LLM 串行执行模式**

用户提供行业关键词后，**直接完成**：
1. **选题发现**（按 topic-discovery 指令执行）
2. **小红书内容生成**（按 xiaohongshu-content-generator 指令执行）
3. **公众号内容生成**（按 wechat-content-generator 指令执行）【v1.1】
4. **视频脚本生成**（按 video-script-generator 指令执行）【v1.2】

---

## 🎯 执行模式

### 参数说明

| 参数 | 值 | 说明 |
|------|----|----|
| `--period` | `day` / `week` | 日更（1个选题）或周更（5个选题） |
| `--count` | 数字 | 精确指定选题数量（覆盖 --period） |
| `--platforms` | `xhs` / `wechat` / `video` / `all` | 目标平台 |
| `--wechat-type` | `subscription` / `service` | 公众号类型（订阅号/服务号） |
| `--wechat-format` | `article` / `card` | 公众号内容格式（长文横版/图文竖版，默认 article） |

### 常用命令

```bash
# 日更模式（默认）：生成1个最优选题的内容
/content-production-pipeline 家居绿植

# 周更模式：生成5个最优选题的内容
/content-production-pipeline 家居绿植 --period week

# 精确指定3个选题
/content-production-pipeline 家居绿植 --count 3

# 小红书 + 公众号双平台
/content-production-pipeline 家居绿植 --platforms xhs,wechat

# 全平台（小红书 + 公众号 + 视频脚本）
/content-production-pipeline 家居绿植 --platforms all

# 公众号长文（横版配图，默认）
/content-production-pipeline 家居绿植 --platforms wechat --wechat-format article

# 公众号图文卡片（竖版配图，类似小红书）
/content-production-pipeline 家居绿植 --platforms wechat --wechat-format card
```

### 默认行为

- **未指定 `--period` 或 `--count`**：默认 `--period day`（1个选题）
- **未指定 `--platforms`**：默认只生成小红书内容
- **未指定 `--wechat-format`**：默认 `article`（长文，横版配图）

---

## 🔄 执行流程

### Phase 1: 选题发现（直接执行）

**直接执行以下步骤（参考 topic-discovery 指令）：**

1. **搜索热点**：用 `search_web` 搜索 5 个维度
   - `[行业] 小红书 爆款 2026`
   - `[行业] 抖音 热门 话题`
   - `[行业] 公众号 10w+`
   - `[行业] 快手 热门视频`
   - `[行业] 视频号 爆款`

2. **分析趋势**：提取热点话题、爆款标题、流量密码

3. **匹配商品**：读取商品库 Excel，为每个选题匹配 1-3 个商品

4. **生成报告**：保存至 `/选题库/日期_关键词_选题报告.md`

5. **解析选题列表**：提取 10 个选题的元数据

**输出示例：**
```
选题列表：
1. 客厅光线不好？这5种耐阴绿植最适合你 [场景痛点类] 🔴🟢
2. 租房党必备！3种绿植让小空间有生机 [场景痛点类] 🔴🟢🔵
3. 新手养绿萝的3个致命误区 [新手避坑类] 🔴🔵🎬
...
```

**选题元数据解析：**
```python
topic_data = {
    'title': '客厅光线不好？这5种耐阴绿植最适合你',
    'type': '场景痛点类',  # 用于智能筛选
    'products': ['琴叶榕', '龟背竹', '天堂鸟'],  # 商品关联
    'platforms': ['xhs', 'wechat'],  # 推荐平台
    'potential': 5  # 爆款潜力 1-5
}
```

---

### Phase 1.5: 选题筛选（v1.3 新增）

> **⚠️ 关键步骤：筛选后才生成配图，避免浪费时间**

**筛选数量确定：**
| 参数 | 筛选数量 |
|------|----------|
| `--period day`（默认） | 1 个最优选题 |
| `--period week` | 5 个最优选题 |
| `--count N` | N 个最优选题 |

**筛选规则：**

```python
def filter_best_topics(topics: list, count: int) -> list:
    """
    筛选最优选题
    
    规则：
    1. 按「爆款潜力」评分排序（5星 > 4星 > 3星）
    2. 确保类型多样性（不连续3个相同类型）
    """
    # 按爆款潜力排序
    sorted_topics = sorted(topics, key=lambda x: x['potential'], reverse=True)
    
    # 确保类型多样性
    selected = []
    type_streak = {}
    
    for topic in sorted_topics:
        if len(selected) >= count:
            break
            
        topic_type = topic['type']
        consecutive = type_streak.get(topic_type, 0)
        
        # 不允许连续3个相同类型
        if consecutive < 2:
            selected.append(topic)
            # 重置其他类型计数
            type_streak = {topic_type: type_streak.get(topic_type, 0) + 1}
    
    return selected
```

**输出示例：**
```
✅ 筛选完成（日更模式）
最优选题：「客厅光线不好？这5种耐阴绿植最适合你」⭐⭐⭐⭐⭐
```

---

### Phase 1.6: 平台选题分配（v1.4 新增）⚠️

> **🚨 核心规则：同一天不同平台必须使用不同选题！**

#### 为什么要使用不同选题？

1. **用户重叠**：部分用户同时关注小红书和公众号，同一天发相同内容会感到重复
2. **平台调性不同**：小红书偏视觉、情绪化；公众号偏深度、知识化
3. **选题适配不同**：根据知识文档 `01-选题策略.md`，不同平台的选题优先级不同

#### 平台选题分配逻辑

```python
def allocate_topics_by_platform(filtered_topics: list, platforms: list) -> dict:
    """
    为不同平台分配不同选题
    
    规则：
    1. 同一天不同平台使用不同选题
    2. 根据平台特性优先匹配最适合的选题
    3. 如果选题数量不足，允许共用但需间隔2天以上
    """
    allocation = {}
    used_topics = set()
    
    # 平台优先选题类型
    platform_priority = {
        'xhs': ['场景痛点类', '新手避坑类', '送礼推荐类'],  # 小红书：视觉化、情绪化
        'wechat': ['养护教程类', '季节时令类', '场景痛点类'],  # 公众号：知识化、深度
        'video': ['养护教程类', '新手避坑类', '对比测评类']   # 视频：动作展示
    }
    
    for platform in platforms:
        priority_types = platform_priority.get(platform, [])
        
        # 按平台优先级筛选未使用的选题
        for topic in filtered_topics:
            if topic['title'] not in used_topics:
                topic_type = topic.get('type', '')
                # 优先匹配的类型加分
                if topic_type in priority_types:
                    allocation[platform] = topic
                    used_topics.add(topic['title'])
                    break
        
        # 如果没有匹配优先类型，使用第一个未使用的
        if platform not in allocation:
            for topic in filtered_topics:
                if topic['title'] not in used_topics:
                    allocation[platform] = topic
                    used_topics.add(topic['title'])
                    break
    
    return allocation
```

#### 执行示例

**输入：** `--platforms xhs,wechat`（小红书 + 公众号双平台）

**执行流程：**
```
1. 选题发现：生成10个选题
2. 选题筛选（Phase 1.5）：筛选出5个最优选题
3. 平台分配（Phase 1.6）：
   - 小红书 → 选题A（场景痛点类，适合视觉化表达）
   - 公众号 → 选题B（养护教程类，适合深度内容）
4. 各平台使用分配的选题生成内容
```

**输出示例：**
```
✅ 平台选题分配完成

📱 小红书 → 「租房党必备！3种绿植让小空间有生机」
   类型：场景痛点类 | 适配度：⭐⭐⭐⭐⭐

📧 公众号 → 「绿萝黄叶的3个原因和解决方法」
   类型：养护教程类 | 适配度：⭐⭐⭐⭐⭐

⚠️ 注意：两个平台使用了不同选题，避免内容重复
```

#### ❌ 禁止行为

```
❌ 错误：同一天小红书和公众号都发「春节必备这5种年宵花」
❌ 错误：只生成1个选题同时用于多个平台
```

#### ✅ 正确行为

```
✅ 正确：小红书发「年宵花推荐」，公众号发「春节绿植养护指南」
✅ 正确：为每个平台单独分配最适合的选题
```

---

### Phase 2: 小红书内容生成（直接执行）

> 📌 只为 **Phase 1.5 筛选出的选题** 生成内容，不处理全部10个

**对每个筛选后的选题直接执行以下步骤（参考 xiaohongshu-content-generator 指令）：**

1. **生成文案**：标题（≤20字）+ 正文（5-6要点）+ 话题标签（8-10个）
2. **选择配图风格**：根据选题类型选择（dreamy-photo / cozy-sketch 等）
3. **生成配图**：调用云雾 API 生成 1 封面 + 5 正文图
4. **保存文件**：创建子文件夹，保存 .md + .png 文件
5. **记录状态**：成功/失败

**输出：**
```
/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/小红书/
├── 2026-01-14_客厅光线不好这5种耐阴绿植最适合你/
│   ├── 2026-01-14_客厅光线不好这5种耐阴绿植最适合你.md
│   ├── 客厅光线不好这5种耐阴绿植最适合你_封面.png
│   └── ...
├── 2026-01-14_租房党必备3种绿植让小空间有生机/
│   └── ...
```

---

### Phase 3: 公众号内容生成（v1.1 实现）✅

**激活条件：** `--platforms` 包含 `wechat` 或 `all`

**智能筛选逻辑：**
```python
def filter_topics_for_wechat(topics: list, max_count: int = 5) -> list:
    """
    筛选适合公众号的选题
    
    优先级（转化率排序）：
    1. 场景痛点类 ⭐⭐⭐⭐⭐
    2. 新手避坑类 ⭐⭐⭐⭐
    3. 季节时令类 ⭐⭐⭐
    
    排除：
    - 养护知识类（订阅号转化率低）
    - 纯产品推荐类（服务号更合适）
    """
    priority_keywords = {
        '场景痛点类': 5,
        '新手避坑类': 4,
        '季节时令类': 3,
        '送礼推荐类': 2,
    }
    
    # 排除关键词
    exclude_keywords = ['纯知识', '植物百科']
    
    scored = []
    for topic in topics:
        # 检查是否应排除
        if any(kw in topic.get('title', '') for kw in exclude_keywords):
            continue
        
        # 计算分数
        score = priority_keywords.get(topic.get('type', ''), 1)
        scored.append((score, topic))
    
    scored.sort(key=lambda x: x[0], reverse=True)
    return [t[1] for t in scored[:max_count]]
```

**执行步骤（直接执行，参考 wechat-content-generator 指令）：**
1. ✅ 智能筛选：从选题列表中筛选适合公众号的选题
2. ✅ 确定平台类型：订阅号（默认）或服务号
3. ✅ **生成文案**：标题（≤25字）+ 摘要（≤25字）+ 正文
4. ✅ **生成配图**：调用云雾 API（1封面 + 3正文图，横版）
5. ✅ **推送草稿箱**：执行以下命令
   ```bash
   cd /Users/dj/.claude/skills/wechat-publish
   node test.js "<生成的Markdown文件绝对路径>"
   ```
6. ✅ **验证推送**：检查命令输出包含 `✓ 推送成功` 和 `草稿ID`
7. ✅ **记录结果**：推送结果.json 中 status 必须为 `"success"`

**输出：**
```
/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/订阅号/
├── 2026-01-14_客厅光线不好这5种耐阴绿植最适合你/
│   ├── 2026-01-14_客厅光线不好这5种耐阴绿植最适合你.md
│   ├── cover.png
│   ├── 01.png, 02.png, 03.png
│   └── 推送结果.json  # 包含 media_id 和草稿箱链接
```

**订阅号 vs 服务号处理差异：**

| 维度 | 订阅号（默认） | 服务号 |
|------|----------------|--------|
| 筛选数量 | 最多5个选题 | 最多2个选题 |
| 内容风格 | 干货输出、场景化 | 导购为主、商品突出 |
| CTA方向 | 企业微信 + 服务号引导 | 商品卡片 + 小程序 |
| 商品植入 | 文末软推荐（0-1个） | 正文关联（1-3个） |
| 推送行为 | 自动推送草稿箱 | 自动推送草稿箱 |

---

### Phase 4: 视频脚本生成（v1.2 实现）✅

**激活条件：** `--platforms` 包含 `video` 或 `all`

**智能筛选逻辑：**
```python
def filter_topics_for_video(topics: list, max_count: int = 3) -> list:
    """
    筛选适合视频的选题
    
    优先级：
    1. 养护教程类 → 知识分享视频 ⭐⭐⭐⭐⭐
    2. 场景类 → 生活感悟视频 ⭐⭐⭐⭐
    3. 对比测评类 → 知识分享视频 ⭐⭐⭐
    
    排除：
    - 纯文字类（无视觉展示价值）
    - 复杂长篇类（不适合短视频）
    """
    video_priority = {
        '养护教程类': 5,
        '新手避坑类': 4,
        '场景类': 4,
        '对比测评类': 3,
        '场景痛点类': 3,
    }
    
    scored = []
    for topic in topics:
        topic_type = topic.get('type', '')
        score = video_priority.get(topic_type, 1)
        
        # 包含教程/方法/技巧关键词加分
        title = topic.get('title', '')
        if any(kw in title for kw in ['教程', '方法', '技巧', '误区', '攻略']):
            score += 1
        
        scored.append((score, topic))
    
    scored.sort(key=lambda x: x[0], reverse=True)
    return [t[1] for t in scored[:max_count]]
```

**执行步骤（直接执行，参考 video-script-generator 指令）：**
1. ✅ 智能筛选：从选题列表中筛选适合视频的选题（最多3个）
2. ✅ 类型判断：自动判断视频类型（知识分享/生活感悟）
3. ✅ **生成分镜脚本**：场景 + 画面描述 + AI图片提示词
4. ✅ **生成逐字稿**：口语化表达 + 场景注解
5. ✅ **生成配图**：调用云雾 API 生成分镜配图
6. ✅ **记录结果**：每个选题的脚本路径

**视频类型自动判断：**
```python
def detect_video_type(topic: dict) -> str:
    """
    根据选题自动判断视频类型
    
    Returns:
        'knowledge' - 知识分享类（教程、干货）
        'insight' - 生活感悟类（情感、治愈）
    """
    title = topic.get('title', '')
    topic_type = topic.get('type', '')
    
    knowledge_keywords = ['教程', '技巧', '方法', '误区', '攻略', '步骤', '原理']
    insight_keywords = ['感悟', '治愈', '人生', '生活', '发现', '慢', '陪伴']
    
    if any(kw in title for kw in knowledge_keywords) or topic_type in ['养护教程类', '新手避坑类']:
        return 'knowledge'
    elif any(kw in title for kw in insight_keywords) or topic_type == '生活感悟类':
        return 'insight'
    else:
        return 'knowledge'  # 默认知识分享
```

**输出：**
```
/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/视频脚本/
├── 2026-01-14_新手养绿萝的3个致命误区_通用/
│   ├── 2026-01-14_新手养绿萝的3个致命误区_分镜脚本.md
│   ├── 2026-01-14_新手养绿萝的3个致命误区_逐字稿.md
│   ├── images/
│   │   ├── 01_开场.png
│   │   ├── 02_误区1.png
│   │   └── ...
```

---

## 📊 进度追踪（使用 TodoWrite）

**执行过程中实时更新：**

```
[ ] Phase 1: 选题发现
[ ] Phase 2: 小红书内容生成
[ ] Phase 3: 公众号内容生成
[ ] Phase 4: 视频脚本生成
[ ] 生成执行报告
```

**示例更新：**
```
[x] Phase 1: 选题发现（10个选题）
[/] Phase 2: 小红书内容生成（5/10）
    [x] 选题1: 客厅光线不好？这5种耐阴绿植最适合你 ✅
    [x] 选题2: 租房党必备！3种绿植让小空间有生机 ✅
    [x] 选题3: 新手养绿萝的3个致命误区 ✅
    [x] 选题4: 春季换盆全攻略 ✅
    [/] 选题5: 办公室绿植推荐 🔄
    [ ] 选题6-10...
[ ] Phase 3: 公众号内容生成
[ ] Phase 4: 视频脚本生成
```

---

## 🛠️ 错误处理与质量验证

**原则：单个失败不影响整体，但必须验证质量**

### 错误处理表

| 错误场景 | 处理方式 | 是否继续 |
|----------|----------|----------|
| 选题发现失败 | 终止流水线，报告错误 | ❌ |
| 单个小红书内容生成失败 | 记录错误，继续下一个 | ✅ |
| 单个公众号内容生成失败 | 记录错误，继续下一个 | ✅ |
| 单个视频脚本生成失败 | 记录错误，继续下一个 | ✅ |
| 推送草稿箱失败 | 记录错误，文件已保存本地 | ✅ |
| 配图生成失败 | **重试3次**，全部失败后跳过该图 | ✅ |
| 商品匹配失败 | 跳过商品植入，继续生成 | ✅ |

---

### 质量验证步骤（v1.3 新增）

**1️⃣ 配图生成验证**

每张图片生成后必须验证：
```python
def verify_image(image_path: str) -> bool:
    """验证配图是否生成成功"""
    import os
    
    # 检查文件存在
    if not os.path.exists(image_path):
        print(f"❌ 配图未生成: {image_path}")
        return False
    
    # 检查文件大小（至少 10KB）
    size = os.path.getsize(image_path)
    if size < 10000:
        print(f"❌ 配图异常（文件过小）: {image_path}, {size} bytes")
        return False
    
    print(f"✅ 配图验证通过: {image_path}")
    return True
```

**2️⃣ 公众号推送前验证**

推送前必须检查元信息已清理：
```python
def verify_clean_before_push(content: str) -> bool:
    """验证文档是否已清洗干净"""
    dirty_patterns = [
        "**发布平台：**", "**发布时间：**", "**选题类型：**",
        "**目标人群：**", "**本文字数：**", "**阅读时间：**"
    ]
    
    for pattern in dirty_patterns:
        if pattern in content:
            return False
    return True

# 如果验证失败，必须先清洗再推送
if not verify_clean_before_push(content):
    content = clean_metadata(content)
```

**3️⃣ 失败重试机制**

```python
def retry_on_failure(func, max_retries=3, delay=5):
    """API 失败时自动重试"""
    for attempt in range(max_retries):
        try:
            return func()
        except Exception as e:
            print(f"⚠️ 第 {attempt+1} 次尝试失败: {e}")
            if attempt < max_retries - 1:
                time.sleep(delay)
    return None  # 全部失败
```

**错误日志：**
```
/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/
└── pipeline_error_log_20260114.md
```

**错误日志格式：**
```markdown
# 流水线错误日志

**执行时间：** 2026-01-14 15:30:00

## 错误列表

| 阶段 | 选题 | 错误类型 | 错误信息 |
|------|------|----------|----------|
| Phase 2 | 选题X | API超时 | 图片生成超时（120s） |
| Phase 3 | 选题Y | 推送失败 | 草稿箱API返回403 |
```

---

## 📈 执行报告

**流水线完成后输出：**

```markdown
# 内容生产流水线执行报告

**执行时间：** 2026-01-14 15:30:00
**行业关键词：** 家居绿植
**执行模式：** 全平台（xhs + wechat + video）
**选题数量：** 10

---

## 📊 执行概览

| 阶段 | 输入 | 成功 | 失败 | 成功率 |
|------|------|------|------|--------|
| 选题发现 | 1 | 1 | 0 | 100% |
| 小红书内容 | 10 | 8 | 2 | 80% |
| 公众号内容 | 5 | 5 | 0 | 100% |
| 视频脚本 | 3 | 3 | 0 | 100% |

---

## 🔴 小红书内容生成

✅ 成功：8 个
❌ 失败：2 个
📁 输出目录：/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/小红书/

### 成功列表
1. ✅ 客厅光线不好？这5种耐阴绿植最适合你
2. ✅ 租房党必备！3种绿植让小空间有生机
...

### 失败列表
1. ❌ 选题X - 错误：API超时
2. ❌ 选题Y - 错误：图片生成失败

---

## 🟢 公众号内容生成（v1.1）

✅ 成功：5 个
📁 输出目录：/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/订阅号/
📤 草稿箱链接：[点击查看](https://mp.weixin.qq.com/...)

### 成功列表
1. ✅ 客厅光线不好？这5种耐阴绿植最适合你 (media_id: xxx)
2. ✅ 租房党必备！3种绿植让小空间有生机 (media_id: xxx)
...

---

## 🎬 视频脚本生成（v1.2）

✅ 成功：3 个
📁 输出目录：/Users/dj/Documents/slowseasons AI工厂/内容发布/发布记录/2026/视频脚本/

### 成功列表
1. ✅ 新手养绿萝的3个致命误区 [知识分享类] 12分镜
   - 分镜脚本：/视频脚本/2026-01-14_新手养绿萝误区/分镜脚本.md
   - 逐字稿：/视频脚本/2026-01-14_新手养绿萝误区/逐字稿.md
   - 配图：12张

---

## 📦 总结

- **总耗时**：45分钟
- **总成功率**：90%
- **生成文件数**：
  - 小红书：80个（8选题 × 10文件）
  - 公众号：20个（5选题 × 4文件）
  - 视频脚本：36个（3选题 × 12文件）
```

---

## 🎛️ 高级配置

### 批量处理策略

**执行顺序（串行）：**
```
选题发现 → 小红书生成 → 公众号生成 → 视频脚本生成
    ↓           ↓              ↓              ↓
  单次调用   逐个选题串行    逐个选题串行    逐个选题串行
```

**原因：**
- API限流控制（Midjourney、云雾API）
- 保持选题处理顺序
- 便于错误追踪和恢复

**重试机制：**
- API失败：自动重试3次
- 超时时间：每个选题最多10分钟
- 重试间隔：5秒、10秒、30秒

### 智能筛选总览

| 平台 | 筛选数量 | 优先类型 | 排除类型 |
|------|----------|----------|----------|
| 小红书 | 全部（按count） | 场景痛点、新手避坑 | 无 |
| 订阅号 | 最多5个 | 场景痛点、新手避坑、季节时令 | 养护知识类 |
| 服务号 | 最多2个 | 导购类、新品介绍 | 纯内容类 |
| 视频 | 最多3个 | 养护教程、场景类 | 纯文字类 |

---

## 📝 使用示例

### 示例1：快速启动（小红书单平台）
```
用户: /content-production-pipeline 春季阳台种植
```

### 示例2：双平台生产
```
用户: /content-production-pipeline 办公室绿植 --platforms xhs,wechat
```

### 示例3：只处理3个选题
```
用户: /content-production-pipeline 懒人养花 --count 3
```

### 示例4：全平台生产
```
用户: /content-production-pipeline 送礼绿植推荐 --platforms all
```

### 示例5：仅生成视频脚本
```
用户: /content-production-pipeline 绿萝养护 --platforms video
```

### 示例6：服务号内容生成
```
用户: /content-production-pipeline 新品发布 --platforms wechat --wechat-type service
```

---

## 🔗 依赖 Skill

| 技能 | 版本要求 | 用途 | 状态 |
|------|----------|------|------|
| `topic-discovery` | v3.0+ | 选题发现 + 商品匹配 | ✅ 已集成 |
| `xiaohongshu-content-generator` | v4.0+ | 小红书图文生成 | ✅ 已集成 |
| `wechat-content-generator` | v3.0+ | 公众号图文生成 + 推送 | ✅ 已集成 |
| `video-script-generator` | v2.0+ | 视频分镜脚本 + 逐字稿 | ✅ 已集成 |
| `product-catalog` | - | 商品库查询 | ✅ 间接依赖 |

---

## 📦 输出目录结构

```
/Users/dj/Documents/slowseasons AI工厂/
├── 选题库/
│   └── 2026-01-14_家居绿植_选题报告.md
├── 内容发布/发布记录/2026/
│   ├── 小红书/
│   │   ├── 2026-01-14_选题1/
│   │   ├── 2026-01-14_选题2/
│   │   └── ...
│   ├── 订阅号/                          # v1.1 新增
│   │   ├── 2026-01-14_选题1/
│   │   │   ├── *.md
│   │   │   ├── cover.png
│   │   │   └── 推送结果.json
│   │   └── ...
│   ├── 服务号/                          # v1.1 新增
│   │   └── ...
│   ├── 视频脚本/                        # v1.2 新增
│   │   ├── 2026-01-14_选题_通用/
│   │   │   ├── *_分镜脚本.md
│   │   │   ├── *_逐字稿.md
│   │   │   └── images/
│   │   └── ...
│   ├── pipeline_report_20260114.md      # 执行报告
│   └── pipeline_error_log_20260114.md   # 错误日志（如有）
```

---

## 📁 临时文件管理

**规则：**
- ✅ 所有临时文件保存到 `.tmp/` 文件夹
- ✅ 任务完成后自动清理
- ❌ 禁止临时文件散落在工作目录中

**临时文件目录：**
```
/内容发布/发布记录/2026/
└── .tmp/
    ├── search_cache_*.json
    ├── progress_*.json
    └── draft_*.md
```

---

## 🚀 执行命令

**默认模式（小红书单平台）：**
```
/content-production-pipeline [行业关键词]
```

**指定平台：**
```
/content-production-pipeline [行业关键词] --platforms xhs,wechat,video
```

**指定数量：**
```
/content-production-pipeline [行业关键词] --count 3
```

**全平台：**
```
/content-production-pipeline [行业关键词] --platforms all
```

**服务号模式：**
```
/content-production-pipeline [行业关键词] --platforms wechat --wechat-type service
```

---

## 📌 注意事项

1. **执行时间**：
   - 小红书单平台（10选题）：约 30-60 分钟
   - 双平台：约 45-90 分钟
   - 全平台：约 60-120 分钟

2. **API 限制**：
   - Midjourney API 并发限制
   - 云雾 API 速率限制
   - 微信 API 每日调用限制

3. **存储空间**：
   - 小红书：每选题约 10-20MB
   - 公众号：每选题约 5-10MB
   - 视频脚本：每选题约 15-30MB

4. **网络稳定**：确保网络连接稳定，避免中途中断

---

## ❌ Anti-Patterns（禁止行为）

**禁止示例1：中途询问确认**
```
❌ "已发现10个选题，是否开始生成小红书内容？"
❌ "小红书内容已完成，要继续生成公众号内容吗？"
```
→ 应根据 `--platforms` 参数自动执行全流程

**禁止示例2：跳过已启用的阶段**
```
❌ 用户指定 --platforms all，但跳过视频脚本生成
❌ 遇到单个错误后终止整个流水线
```
→ 单个失败记录日志，继续处理下一个

**禁止示例3：不输出执行报告**
```
❌ 流水线完成后没有汇总报告
❌ 没有记录成功/失败统计
```
→ 必须输出完整的执行报告

---

## 🔄 版本历史

### v1.4 (2026-01-15) - 当前版本
- ✅ **平台选题分配**：同一天不同平台必须使用不同选题（Phase 1.6）
- ✅ **选题适配优化**：根据平台特性自动匹配最适合的选题类型
- ✅ **推送验证强化**：草稿箱推送后增加结果验证步骤

### v1.3 (2026-01-14)
- ✅ **选题筛选优化**：按爆款潜力和类型多样性筛选

### v1.2 (2026-01-14)
- ✅ **视频脚本集成**：调用 `video-script-generator` 生成分镜脚本 + 逐字稿
- ✅ **智能类型判断**：自动判断知识分享 vs 生活感悟
- ✅ **迁移到小静的skills**：统一技能管理方式

### v1.1 (2026-01-14)
- ✅ **公众号内容集成**：调用 `wechat-content-generator` 生成推文
- ✅ **订阅号/服务号支持**：`--wechat-type` 参数
- ✅ **智能选题筛选**：按平台优化选题匹配
- ✅ **草稿箱自动推送**：生成后自动推送至微信草稿箱

### v1.0 (2026-01-14)
- ✅ 支持选题发现 + 小红书内容生成
- ✅ 支持批量处理和进度追踪
- ✅ 支持错误恢复和日志记录

